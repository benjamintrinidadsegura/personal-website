import type { Metadata } from "next";
import Link from "next/link";

import { NewsletterCta } from "@/components/newsletter/newsletter-cta";
import { getPublishedWriting } from "@/lib/writing/queries";

const description = "Essays und Notes über People, Work, Building, Life und Ideas.";
const dateFormatter = new Intl.DateTimeFormat("de-DE", { day: "numeric", month: "long", year: "numeric", timeZone: "Europe/Berlin" });

export const metadata: Metadata = {
  title: "Writing | bts.online", description, alternates: { canonical: "/writing" },
  openGraph: { type: "website", locale: "de_DE", url: "/writing", siteName: "bts.online", title: "Writing | bts.online", description, images: [{ url: "/og.png", width: 1732, height: 909, alt: "Benjamin Trinidad Segura – Digital HQ" }] },
  twitter: { card: "summary_large_image", title: "Writing | bts.online", description, images: ["/og.png"] },
};

export default async function WritingPage() {
  const articles = await getPublishedWriting();
  const [featured, ...latest] = articles;
  return <article className="section-lines relative overflow-hidden px-5 pb-24 pt-28 sm:px-8 sm:pt-36"><div aria-hidden="true" className="absolute inset-x-0 top-0 h-[52rem] bg-[radial-gradient(circle_at_74%_18%,rgba(53,208,229,0.15),transparent_28rem),radial-gradient(circle_at_18%_44%,rgba(255,122,0,0.06),transparent_22rem)]" /><div className="relative mx-auto max-w-[90rem]"><nav aria-label="Breadcrumb" className="font-mono text-xs text-slate-400"><Link href="/" className="inline-flex min-h-11 items-center hover:text-white">Digital HQ</Link> / <span aria-current="page" className="text-[#35d0e5]">Writing</span></nav><header className="grid min-h-[58svh] items-center gap-12 border-b border-white/15 py-16 lg:grid-cols-[1.2fr_0.8fr]"><div><p className="text-xs font-black uppercase tracking-[0.3em] text-[#35d0e5]">Writing / Field Notes</p><h1 className="mt-7 text-[clamp(3.7rem,11vw,9rem)] font-black leading-[0.84] tracking-[-0.06em] text-white">Thoughts<br />worth keeping.</h1></div><p className="border-l border-[#ff9a3d] pl-7 text-xl font-bold leading-9 text-slate-200">Gedanken über Arbeit, Identität, Mut und die Geschichten, die wir über uns selbst erzählen.</p></header>{featured ? <section aria-labelledby="featured-writing-title" className="border-b border-white/15 py-20 sm:py-28"><p className="font-mono text-xs uppercase tracking-[0.22em] text-[#ff9a3d]">Featured Writing</p><Link href={`/writing/${featured.slug}`} className="group mt-8 grid gap-8 border border-white/10 bg-white/[0.02] p-7 transition hover:border-[#35d0e5]/50 sm:p-10 lg:grid-cols-[1fr_0.35fr]"><div><div className="flex flex-wrap gap-3 font-mono text-xs uppercase tracking-[0.16em] text-[#35d0e5]"><span>{featured.contentType}</span><span aria-hidden="true">·</span><span>{featured.topics.join(" · ")}</span></div><h2 id="featured-writing-title" className="mt-7 break-words text-4xl font-black leading-tight text-white sm:text-6xl">{featured.title}</h2><p className="mt-6 max-w-3xl text-lg leading-8 text-slate-300">{featured.excerpt}</p></div><div className="flex flex-col justify-end border-t border-white/10 pt-6 lg:border-l lg:border-t-0 lg:pl-8 lg:pt-0"><time dateTime={featured.publishedAt} className="font-mono text-xs text-slate-500">{dateFormatter.format(new Date(featured.publishedAt))}</time><p className="mt-3 text-sm text-slate-400">{featured.readingMinutes} Min. Lesezeit</p><p className="mt-8 font-black text-[#35d0e5]">Read article →</p></div></Link></section> : <section aria-labelledby="empty-writing-title" className="border-b border-white/15 py-20"><h2 id="empty-writing-title" className="text-3xl font-black text-white">The first Writing is taking shape.</h2><p className="mt-4 max-w-2xl leading-7 text-slate-300">Die ersten Essays und Notes werden im BTS Studio vorbereitet.</p></section>}{latest.length ? <section aria-labelledby="latest-writing-title" className="py-20 sm:py-28"><h2 id="latest-writing-title" className="text-4xl font-black text-white sm:text-6xl">Latest Writing</h2><ol className="mt-12 border-t border-white/15">{latest.map((article) => <li key={article.id}><Link href={`/writing/${article.slug}`} className="group grid gap-5 border-b border-white/15 py-9 transition hover:bg-white/[0.025] sm:px-4 lg:grid-cols-[0.25fr_1fr_0.3fr] lg:items-center"><div><p className="font-mono text-xs uppercase tracking-[0.18em] text-[#35d0e5]">{article.contentType}</p><p className="mt-2 text-sm text-slate-500">{article.topics.join(" · ")}</p></div><div><h3 className="break-words text-2xl font-black text-white sm:text-3xl">{article.title}</h3><p className="mt-3 max-w-2xl leading-7 text-slate-300">{article.excerpt}</p></div><div className="lg:text-right"><time dateTime={article.publishedAt} className="font-mono text-xs text-slate-500">{dateFormatter.format(new Date(article.publishedAt))}</time><p className="mt-3 text-sm text-slate-400">{article.readingMinutes} Min.</p></div></Link></li>)}</ol></section> : null}<NewsletterCta /></div></article>;
}
